#include <iostream>
#include <string>
#include <set>
#include <ifstream>

using namespace std;

int main() {
  // Files
  string path = "/Users/spencergordon/Documents/Programming/CPP_Programming/STL Problems/input.txt"
  ifstream myfile;

  set s;

  if (myfile.is_open()) {
    if (myfile.is_open())
    {
      while ( getline (myfile,line) )
      {

      }
      myfile.close();
    }
  }
}
